# Instructions for Claude

- This repo is Charlie's portfolio site, deployed via GitHub Pages from the `main` branch (`origin` = `Charliegettinghub/Main-Site-`). Live at https://charliegettinghub.github.io/Main-Site-/.
- Whenever asked to update the site (content, copy, styling, etc.), commit the change and push it to `origin/main` automatically — no need to ask for confirmation each time.
